\subsection{Task: TASKNAME} % Choose a short yet descriptive (!) name, if the "filename" does not provide an adequate description
% put general information about the task here: what does it do, what is important to know when using this task, etc... (have a look at existing tasks if in doubt) 

\subsubsection{Example Input}
% provide at least one example input; if applicable, show the use of the most commonly used keywords
\begin{lstlisting}
+task NAME
  ...
-task
\end{lstlisting}
\subsubsection{Basic Keywords}
% list all basic keywords for the task, provide a short description of the keyword, allowed options/values (and format if necessary) and the default 
% use \ttt{...} with keyword options/values!
% if a task uses common sub-sblock settings (embedding/local correlation), use \hyperref[sec:scb:emb/lc]{emb/lc} to provide a link to this section
\begin{description}
    \item [\texttt{name}]\hfill \\
    %list all aliases

    \item [\texttt{KEYWORDNAME}]\hfill \\
    %description
\end{description}
    
\subsubsection{Advanced Keywords}
% list advanced keywords if available, delete this section otherwise. Use the same format as basic keywords
\begin{description}
    \item [\texttt{KEYWORDNAME}]\hfill \\
    % description
    
\end{description}

%save this file under the same name as the tasks .h/.cpp files and add \input{input/tasks/TaskNameTask.tex} to tasks.tex
